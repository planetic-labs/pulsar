[project]
name = "videodb-backup-tool"
version = "0.1.0"
description = "Standalone backup and restore tool for VideoDB"
dependencies = [
    "boto3>=1.34.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.1",
]
requires-python = ">=3.12"

[tool.uv]
package = false
