# Pulsar - Environment Configuration Example
# Copy this file to .env and fill in the values.

# --- App Security & Settings ---
# Access token for UI and API authentication
APP_ACCESS_TOKEN=change-me-to-something-secure
# Secret key for session encryption
SESSION_SECRET_KEY=generate-a-long-random-string-here

# Application directories (absolute paths recommended for Docker)
APP_STORAGE_DIR=/app/storage
APP_DATA_DIR=/app/data
DISK_SPACE_BUFFER_GB=3
# Max audio file size in MB to remain in WAV before compression to OGG
# Comma-separated list of keywords to exclude files from indexing
EXCLUDE_KEYWORDS=ГАЛЕРЕЯ

# GitHub Integration (optional, for feedback form)
GITHUB_PAT=
GITHUB_REPO=planetic-labs/pulsar

# --- SQLite Settings ---
SQLITE_DB_PATH=/app/data/pulsar.db

# --- Manticore Settings ---
MANTICORE_URL=http://manticore:9308
MANTICORE_TABLE=chunks

# --- S3 Backup Settings (optional) ---
S3_ENDPOINT_URL=https://s3.your-provider.com
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET_NAME=
S3_REGION_NAME=ru-1
MAX_BACKUPS=10
COMPOSE_FILE=docker-compose.yml

# --- Telegram Notifications Settings (optional) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Deepgram Settings (Speech-to-Text) ---
DEEPGRAM_API_KEY=your-deepgram-api-key
DEEPGRAM_PROJECT_ID=your-project-id
DEEPGRAM_MODEL=nova-3
DEEPGRAM_LANGUAGE=ru

# --- Google Drive API (Service Account) ---
# Path to your service account JSON key file
GOOGLE_DRIVE_CREDENTIALS_PATH=/app/config/service-key.json
GOOGLE_DRIVE_DOWNLOAD_DIR=/app/downloads
GOOGLE_DRIVE_SCOPES=https://www.googleapis.com/auth/drive.readonly

# --- Embedding Service (Remote Infinity/vLLM/OpenAI/OpenRouter) ---
# Provider: 'custom' (Infinity/vLLM) or 'openai' (OpenAI/OpenRouter)
EMBEDDING_PROVIDER=custom
# URL to your embedding service
EMBEDDING_API_URL=http://infinity:7997/v1
EMBEDDING_API_TOKEN=your-embedding-api-token
EMBEDDING_MODEL_ID=BAAI/bge-m3
EMBEDDING_DIMENSION=1024
EMBEDDING_CACHE_LRU_SIZE=20
# Specify OpenRouter provider slugs separated by commas to force routing (e.g. together, fireworks, lepton)
EMBEDDING_OPENROUTER_PROVIDERS=

# --- Optional Settings ---
# Hugging Face token (if needed for models)
HF_TOKEN=

# --- Ark Messenger Auth Integration (Optional) ---
# URL to Ark Messenger JWKS endpoint
ARK_JWKS_URL=https://api.yourdomain.com/.well-known/jwks.json
# HMAC-SHA256 secret key for verification of session revocation webhooks
ARK_WEBHOOK_SECRET=my-very-secure-secret-key-1

